class Solution {
public:
    bool isPowerOfTwo(int n) {
        if(n == 1){
            return true;
        }
        if (n <= 0 || n % 2 != 0) return false;  // negative, zero or odd numbers are not power of two

        return isPowerOfTwo(n / 2);
    }
};
